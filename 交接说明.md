# 教务 Excel 质检工具 · 项目交接说明

> 面向接手人：本文档说明「这个工具做什么、靠什么技术实现、代码怎么组织、如何零改动接入公司 Sealos DevBox 里运行的网页」。
> 目标环境：**Sealos DevBox（云端开发容器）**，不是应用管理（App Launchpad）。
> 对应代码版本：`d291e36`（main 分支）。

---

## 0. 一句话概览

一个**自带前端、零外部依赖**的 Python Web 小工具：教务人员上传 Excel/CSV，系统按业务线执行一批数据合规质检规则，页面回显部门维度的统计，并生成一份带高亮标注的 Excel 结果供下载。

它**不是** WSGI/ASGI 应用（没有 Flask/FastAPI/Django），而是一个直接 `python3 webapp.py` 起来的独立 HTTP 进程。这一点决定了它在 Sealos DevBox 里的启动与接入方式（见第 5 节）。

---

## 1. 核心功能

### 1.1 两条业务线

| 业务类型 | `business_type` | 需要上传的文件 | 执行的规则集 |
|---|---|---|---|
| 班课业务 | `class` | 1 份班级配课表（classlesson）+ 3 份班级信息表（本月&次月开课 / 本月设班 / 本月结课） | 班级信息 21 条 + 配课 9 条 |
| 高端业务 | `premium` | 仅 1 份 PCLV 高端配课表 | 配课 4 条（不检查班级信息） |

业务类型、部门、配课表类型在**后端二次校验**（`qc_service.validate_business_selection`）：班课只接受 classlesson，高端只接受 pclv，错传直接报错；`高中一对一部` 被强制绑定到高端业务。

### 1.2 使用流程

1. 选业务类型 → 页面自动切换可选的部门列表与文件入口（纯前端，无请求）。
2. 选部门（一次选择，同时用于班级信息表和配课表的筛选）+ 质检月份 + 上传文件。
3. 提交 → 后端同步执行质检 → 返回 JSON。
4. 页面展示：所选部门的记录数 / 异常数 / 规则命中排行 / 部门汇总表。
5. 点击下载 → `教务质检结果-部门.xlsx`，**只包含所选部门的明细行**。

### 1.3 关键处理逻辑

- **三份 CSV 自动合并**：自动兼容 `UTF-8` / `UTF-8 BOM` / `GB18030` 编码，校验三份文件列结构一致，合并成固定 Sheet 名的 `classinfo` 工作簿。仍兼容直接上传已合并的单个 `classinfo` 工作簿。
- **标化部门映射**（`labeling.py`）：把源数据的"标准部门"映射为统一口径的"标化部门"，规则含素养智学部按科目拆分为 素养/素质/小学学习机、智慧学习部→中学学习机、校区名命中湖南地级市或"省域网络"→创新。PCLV **不做**这层映射，直接沿用"标准部门名称"。
- **结果标注**：明细 Sheet 首列插入 `质检结果反馈`，用 `+` 汇总该行命中的所有异常，黄底红字加粗；表头深色冻结、自动筛选、列宽自适应。
- **部门组合核对** Sheet：输出管理部门与标准部门的对应关系，并带"待核对/匹配/不匹配"下拉，供人工确认。

### 1.4 质检规则清单

**班级信息（21 列 / 20 个函数，`qc.py`）**
续班类型异常、班级标价规范、未设置主带课教师、关联班号异常、审核状态异常、季度与开课日期不符、上课形式&授课方式&授课渠道异常、取消班状态异常、设班时间晚于开课时间、实际开课日期不早于开课日期当月、上课时间（外）未填写、教材发放形式填写错误、封班检查（0人班未封班 / 计费体系需确认）、最小发薪部门为空、班级时长&实际时长异常、未设置上课地点/上课教室、一卡通设置错误、学习机线上班级设置规范、开班保底人数、班级类型错误、疑似非本管理项目下班级

**班级配课表 classlesson（9 列 / 8 个函数，`classlesson_qc.py`）**
异常时间排课、教师考勤异常、学员考勤异常、0人班处理、配课表审核时效异常、授课内容类型异常、课程字段完整性异常、学习机-授课方式标识异常、上课日期不早于开课日期当月

**高端配课表 PCLV（4 列 / 3 个函数，`pclv_qc.py`）**
异常时间排课、教师考勤异常、学员考勤异常、配课表审核时效异常

> 规则业务口径来源：`产品说明书/长沙学校数据质检规则-2026.9.xlsx`、`产品说明书/数据合规质检规则培训-培训.pptx`、三份《新东方学校产品说明书》PDF。改规则前建议先核对这几份文件。

### 1.5 输出 Excel 结构

| Sheet | 内容 |
|---|---|
| 质检参数 | 业务类型、质检月份、质检部门 |
| 部门汇总 | 按 数据类型 × 标化部门 的总记录数、异常记录数、异常率 |
| 班级信息质检 | 仅班课业务；含首列 `质检结果反馈` |
| 配课质检-classlesson / 配课质检-pclv | 含首列 `质检结果反馈` |
| 部门组合核对 | 人工核对用，E 列带下拉 |

---

## 2. 核心技术

| 层面 | 选型 | 说明 |
|---|---|---|
| 语言 | Python 3.9+ | 本地开发 3.9.6；**上限 3.12**（见风险 R1） |
| Web 层 | 标准库 `http.server.ThreadingHTTPServer` + `SimpleHTTPRequestHandler` | 无框架依赖；多线程，每个请求一个线程，同步阻塞执行 |
| 上传解析 | 标准库 `cgi.FieldStorage` | 解析 `multipart/form-data` |
| 数据处理 | `pandas>=1.5,<3` | 全表向量化运算，规则即"读列 → 算布尔掩码 → 写 0/1 列" |
| Excel 读写 | `openpyxl>=3.1,<4` | 读 `.xlsx`；写多 Sheet + 样式（填充/字体/冻结/筛选/列宽/数据验证） |
| 前端 | 原生 HTML + CSS + JS（`web/static/` 三个文件） | 无构建、无 npm、无框架；`fetch` + `FormData` 提交 |
| 存储 | 文件系统 `runtime/jobs/<job_id>/` | 无数据库、无对象存储、无 Redis |
| 配置 | 环境变量 `QC_HOST` / `QC_PORT` | 默认 `127.0.0.1:8000` |
| 测试 | `unittest`（`tests/` 5 个文件） | 无第三方测试依赖 |
| 进程声明 | `Procfile`：`web: python3 webapp.py` | 平台可直接识别 |

**依赖只有 2 个**（`requirements.txt`）：`pandas`、`openpyxl`。

**架构特征**：完全无状态（除任务文件目录），无登录鉴权，无任务队列，无异步。质检在请求线程内同步跑完，大文件耗时较长——接入时务必把网关的读超时放宽。

---

## 3. 项目组织架构

### 3.1 目录树

```
QC/
├── webapp.py                 # 【入口】HTTP 服务：路由、上传落盘、任务目录、下载
├── qc_service.py             # 【编排层】类型识别、业务校验、规则调度、统计、导出 Excel
├── qc.py                     # 【规则层】班级信息 21 条规则（909 行，最大文件）
├── classlesson_qc.py         # 【规则层】班级配课表 9 条规则 + 部门组合核对
├── pclv_qc.py                # 【规则层】PCLV 配课表 4 条规则 + 部门组合核对
├── labeling.py               # 【数据层】标化部门映射规则
├── data_loader.py            # 【数据层】Excel/CSV 读取、多 Sheet 合并去重
├── main.py                   # 【旁路 CLI】仅跑班级信息质检，输出 output/final_result.xlsx
├── classlesson_main.py       # 【旁路 CLI】仅跑配课表打标，输出 output/classlesson_labeled.xlsx
│
├── web/static/
│   ├── index.html            # 页面结构
│   ├── styles.css            # 样式
│   └── app.js                # 交互 + fetch('/api/qc')
│
├── tests/                    # unittest：test_qc / test_classlesson_qc / test_pclv_qc
│                             #           / test_labeling / test_qc_service
├── data/                     # 样例数据（classinfo.xlsx、classlesson.xlsx、pclv.xlsx、3 份 csv）
├── output/                   # CLI 产物（.gitignore）
├── runtime/jobs/             # Web 任务产物，运行时生成（.gitignore）
├── 产品说明书/               # 规则口径依据（PDF / PPTX / XLSX）
│
├── requirements.txt
├── Procfile
├── README.md
└── 交接说明.md               # 本文档
```

### 3.2 分层与依赖方向

```
        ┌──────────────┐        ┌──────────────┐
        │   main.py    │        │classlesson_  │   ← 旁路 CLI，仅供本地跑数，
        │  (CLI 入口)  │        │  main.py     │     不参与 Web，可安全忽略
        └──────┬───────┘        └──────┬───────┘
               │                       │
               └──────────┬────────────┘
                          ▼
  浏览器 ──HTTP──▶ webapp.py ──▶ qc_service.py ──┬──▶ qc.py            (班级信息规则)
                  (HTTP 层)     (编排层)        ├──▶ classlesson_qc.py (班课配课规则)
                                                ├──▶ pclv_qc.py        (高端配课规则)
                                                ├──▶ labeling.py       (标化部门)
                                                └──▶ data_loader.py    (读取/合并/去重)
```

- 依赖**单向向下**，规则层不反向依赖编排层或 HTTP 层，因此规则可单独测试、单独在 CLI 里跑。
- `webapp.py` 只做三件事：解析 multipart、调 `qc_service.run_qc_job()`、回 JSON / 回文件。业务改动基本不需要碰它。

### 3.3 各模块职责速查

| 文件 | 关键导出 | 职责 |
|---|---|---|
| `webapp.py` | `run(host, port)`、`QCRequestHandler` | 路由 `/api/health`、`/api/qc`、`/api/jobs/<id>/download`；静态目录 `web/static`；80MB 上传上限；失败时清理任务目录 |
| `qc_service.py` | `run_qc_job()`、`detect_schedule_type()`、`validate_business_selection()`、`classinfo_required_for_selection()`、`combine_classinfo_files()`、`add_qc_feedback()`、`build_department_summary()` | 全流程编排：识别配课表类型 → 校验业务/部门一致性 → 跑规则 → 部门筛选 → 统计 → 写 Excel |
| `qc.py` | 20 个 `check_*` + `normalize_qc_month()` | 班级信息规则，每条规则新增一列（0/1） |
| `classlesson_qc.py` | 8 个 `check_*` + `build_department_combination_review()` | 班课配课表规则 |
| `pclv_qc.py` | 3 个 `check_*` + `build_department_combination_review()` | 高端配课表规则 |
| `labeling.py` | `apply_standard_department()`、`apply_classlesson_standard_department()` | 标化部门映射，输出"标化部门"列 |
| `data_loader.py` | `load_classlesson_excel()`、`merge_and_deduplicate_excel()` | 读表；后者加 `数据分类` 列并按行去重 |

### 3.4 一次请求的完整数据流

```
POST /api/qc (multipart)
  └─ webapp: 建 runtime/jobs/<uuid>/input/
      ├─ 存 schedule.xlsx / pclv.xlsx
      └─ 三份 csv → combine_classinfo_files → classinfo-自动合并.xlsx
  └─ qc_service.run_qc_job(...)
      ├─ detect_schedule_type()         按关键列名判定 classlesson / pclv
      ├─ normalize_business_type()      兼容老调用；空则按配课表类型推断
      ├─ validate_business_selection()  业务 × 部门 × 配课表类型一致性
      ├─ run_classlesson_qc / run_pclv_qc
      │    └─ load → 打标化部门 → 逐条 check_*（追加 0/1 列）
      ├─ run_classinfo_qc()（仅班课）
      ├─ filter_by_standard_department()  按部门裁剪明细
      ├─ add_qc_feedback()              生成首列"质检结果反馈"
      ├─ build_department_summary()     部门维度统计
      └─ pd.ExcelWriter 写 5 个 Sheet + _format_output_workbook() 美化
  └─ 写 result.json + 返回 JSON（含 download_url）
  └─ GET /api/jobs/<uuid>/download → 按部门名重命名后返回 xlsx
```

### 3.5 接口契约

**`GET /api/health`** → `{"status":"ok","time":"<ISO8601>"}`（适合做 Sealos 存活探针）

**`POST /api/qc`**（`multipart/form-data`）

| 字段 | 必填 | 说明 |
|---|---|---|
| `business_type` | 是 | `class` 或 `premium` |
| `standard_department` | 是 | 部门名，可传 `全部部门` |
| `qc_month` | 是 | `YYYY-MM` |
| `schedule` | 是 | 配课表文件（`.xlsx`/`.xls`） |
| `classinfo_opening` | 班课必填 | 本月&次月开课（`.csv`/`.xlsx`/`.xls`） |
| `classinfo_setup` | 班课必填 | 本月设班 |
| `classinfo_closing` | 班课必填 | 本月结课 |
| `classinfo` | 可选 | 兼容字段：已合并的单工作簿，三份分表都为空时使用 |

成功响应 200（节选）：

```json
{
  "business_type": "class",
  "business_label": "班课业务",
  "schedule_type": "classlesson",
  "qc_month": "2026-09",
  "selected_department": "高中班级部",
  "classinfo_checked": true,
  "classinfo_rows": 1234,
  "schedule_rows": 5678,
  "classinfo_abnormal_rows": 56,
  "schedule_abnormal_rows": 78,
  "department_summary": [{"数据类型":"班级信息","标化部门":"…","总记录数":…,"异常记录数":…,"异常率":…}],
  "rule_stats": [{"rule":"续班类型异常","count":12}],
  "job_id": "<32位hex>",
  "download_url": "/api/jobs/<job_id>/download",
  "created_at": "2026-09-15T10:30:00"
}
```

失败：4xx 返回 `{"error": "中文提示"}`（参数/文件类型/部门不存在等业务错误）；5xx 返回通用提示（真实堆栈只打到 stdout）。

**`GET /api/jobs/<job_id>/download`** → `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`，文件名 `教务质检结果-<部门>.xlsx`（UTF-8 编码）。

---

## 4. 本地运行与测试

```bash
python3 -m pip install -r requirements.txt
python3 webapp.py                      # http://127.0.0.1:8000
QC_HOST=0.0.0.0 QC_PORT=8080 python3 webapp.py

# CLI 旁路（不依赖 Web）
python3 main.py                        # → output/final_result.xlsx
python3 classlesson_main.py            # → output/classlesson_labeled.xlsx

# 测试
python3 -m unittest discover -s tests -v
```

---

## 5. 接入 Sealos DevBox（不改动现有业务代码）

### 5.1 先理解 DevBox 的形态

DevBox 是 Sealos 上的**云端开发容器**：自带运行时、WebIDE / Web 终端、可用 Cursor / VSCode 远程连接，并在创建时通过「网络配置」把某个容器端口暴露成一个**公网地址**（系统自动分配子域名，也支持自定义域名）。

| 对比 | DevBox | 应用管理（App Launchpad） |
|---|---|---|
| 定位 | 开发 / 联调 / 预览环境 | 长期运行的生产服务 |
| 启动方式 | DevBox 内 `entrypoint.sh` 启动进程 | 镜像 + 副本数 |
| 访问 | 创建时配置端口并开启公网访问 | 配置端口 + 外网地址 |
| 持久化 | 开发容器自带工作目录，重启保留；重建环境会丢 | 需显式挂 PVC |
| 交付 | 可「发布版本」打成 OCI 镜像，再一键部署到应用管理 | — |

**对本项目的影响**：不需要 Dockerfile、不需要构建镜像，直接在 DevBox 里 `git clone` + 装依赖 + 写 `entrypoint.sh` 就能跑起来，并通过公网地址被公司网页引用。

> 官方也建议：长期对外提供服务时，最终走「DevBox → 发布版本 → 应用管理」的路径，而不是一直依赖开发态服务。当前先用 DevBox 跑通完全可行，后续想转正只需在 DevBox 里点「发布版本」。

### 5.2 环境约束

| 项 | 要求 |
|---|---|
| Python | **3.9 ~ 3.12**。不要选 3.13+（`cgi` 模块已被移除，`webapp.py` 第 1 行 import 会直接崩） |
| 容器端口 | 建议 `8000`，**必须与 `QC_PORT` 一致**，否则页面打不开 |
| 监听地址 | 必须 `0.0.0.0`，不能用默认的 `127.0.0.1`（DevBox 外网进不来） |
| 可写目录 | `runtime/`（任务结果与上传临时文件） |
| 依赖 | 仅 `pandas` + `openpyxl` |
| 实例数 | DevBox 天然单实例，无多副本一致性问题 |
| 启动命令 | `python3 webapp.py`（`Procfile` 已声明 `web: python3 webapp.py`） |
| 探针 | `GET /api/health` |

### 5.3 在 DevBox 里的部署步骤

**第 1 步 · 创建 DevBox**

- 运行环境：选 **Python**，创建后先确认版本 `python3 --version` 落在 3.9~3.12（若默认是 3.13，改用 `python3.11` 或换基础镜像/运行时版本）。
- 资源：2C4G 足够。
- **网络配置：容器端口填 `8000`，并开启公网访问**（系统会分配一个子域名，例如 `https://xxxx.cloud.sealos.io`，也支持绑自定义域名）。

**第 2 步 · 放代码**

在 WebIDE / Web 终端里（假设工作目录为 `~/QC`）：

```bash
git clone <本仓库地址> ~/QC
cd ~/QC
```

**第 3 步 · 装依赖**

```bash
python3 -m pip install -r requirements.txt
# 国内源可选：
# python3 -m pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

**第 4 步 · 写启动脚本 `entrypoint.sh`**

DevBox 用仓库/工作目录下的 `entrypoint.sh` 拉起应用，且**只负责启动、不含构建步骤**，必须**前台阻塞运行**（不要用 `&` 放后台，否则容器认为进程退出）。

```bash
#!/bin/bash
set -e
cd ~/QC

export QC_HOST=0.0.0.0
export QC_PORT=8000          # 必须与 DevBox 网络配置里的容器端口一致
export PYTHONUNBUFFERED=1

exec python3 webapp.py
```

```bash
chmod +x entrypoint.sh
```

> 环境变量也可以配在 DevBox 的环境变量面板里；写在 `entrypoint.sh` 里更不容易丢。二者都有时以实际执行顺序为准，建议只用一处避免混淆。

**第 5 步 · 启动并自检**

```bash
./entrypoint.sh                       # 或直接在 DevBox 面板点「重启/运行」
curl http://127.0.0.1:8000/api/health # 容器内先自测
```

再用浏览器打开 DevBox 详情页面给出的公网地址，确认页面正常、控制台无报错。
**若页面打不开但进程已启动**，只查两件事：监听地址是不是 `0.0.0.0`、DevBox 暴露端口是不是 `8000`。

### 5.4 常见情形：公司网页就在同一个 DevBox 里

如果公司网页的代码也跑在**同一个 DevBox 容器**内，那么质检工具和公司网页是两个端口上的两个进程，互不干扰、互不改代码：

| 进程 | 端口 | DevBox 网络配置 |
|---|---|---|
| 公司网页（Node / Python / 其他） | 例如 `3000` | 已配置并暴露 |
| 教务质检工具 | `8000` | 需**新增一个端口**并暴露（DevBox 支持多端口） |

- **同一容器内互相访问**：公司网页的**后端**可直接用 `http://127.0.0.1:8000`（`QC_HOST=0.0.0.0` 已覆盖回环访问），不需要出公网，最快也最安全。
- **公司网页的前端**若要引用质检页面，仍需用浏览器能访问到的地址——即 DevBox 为 8000 端口分配的公网地址（浏览器不认容器内的 `127.0.0.1`）。
- 两个进程都要在 `entrypoint.sh` 里拉起，且**只能有一个前台阻塞**，另一个用 `&` 放后台并加 `wait`：

```bash
#!/bin/bash
set -e
cd ~/QC

export QC_HOST=0.0.0.0
export QC_PORT=8000
export PYTHONUNBUFFERED=1

# 后台拉起质检工具
nohup python3 webapp.py > ~/qc.log 2>&1 &

# 前台拉起公司网页（保持容器不退出）
cd ~/company-web
exec npm run start        # 换成公司网页真实的启动命令
```

> 若公司网页在**另一个 DevBox** 里，则走 5.5 的方案 A（公网地址）或方案 B（同工作空间内可用集群内地址 / 公网地址）。

### 5.5 三种接入姿势（按推荐度排序）

> 下面用 `https://qc-xxxx.cloud.sealos.io` 代指 DevBox 分配的公网地址，实际以 DevBox 详情页显示为准。

#### 方案 A：公网地址 + iframe / 链接跳转 —— 现有代码零改动 ✅ 推荐

DevBox 的公网地址本身就是**域名根路径**，`/styles.css`、`/app.js`、`/api/qc` 这些绝对路径天然可用，因此既不改质检代码、也不改公司站点路由，只加一个入口即可：

```html
<!-- 方式 1：入口跳转，改动最小 -->
<a href="https://qc-xxxx.cloud.sealos.io" target="_blank">教务数据质检</a>

<!-- 方式 2：内嵌到公司页面 -->
<iframe
  src="https://qc-xxxx.cloud.sealos.io"
  style="width:100%;height:900px;border:0"
  allow="fullscreen">
</iframe>
```

- 本服务**未设置** `X-Frame-Options` / `Content-Security-Policy` 响应头，iframe 可正常嵌入。
- 若公司站点配了 CSP，需要把该域名加入 `frame-src` / `connect-src`。
- 若绑定了自定义域名（如 `qc.公司域名.com`），把上面 URL 换掉即可，其余不变。

#### 方案 B：公司网页后端 / 网关直连 API —— 前端完全自研

公司网页不引用质检页面，只把 DevBox 当后端服务：

- **走公网**：`POST https://qc-xxxx.cloud.sealos.io/api/qc`（`FormData`，字段见 3.5），自行渲染 `department_summary` / `rule_stats`，再用返回的 `download_url` 触发下载。
- **走集群内**：若公司网页也部署在同一个 Sealos 工作空间内，可优先用集群内地址访问，避免出公网（更快、也少一层暴露）。集群内服务名可在 DevBox 的 Web 终端里用 `env | grep -i -E 'service|host'` 或 `kubectl get svc` 查看；若不在同一命名空间，直接用公网地址更省事。
- 需要服务端放开 CORS：在 `webapp.py` 的 `end_headers()` 里加 `Access-Control-Allow-Origin` 等响应头并补一个 `do_OPTIONS`——这属于小改动，按需要再评估。

#### 方案 C：Nginx 反代到公司站点子路径 —— 不推荐，需改前端

`web/static/index.html` 里静态资源用的是**绝对路径** `/styles.css`、`/app.js`，`app.js` 里请求的是 `/api/qc`、`/api/jobs/...`。

若一定要挂到公司站点的子路径（如 `/qc/`），必须改这 3~4 处为相对路径，属于"改动现有代码"。**建议改用方案 A**，用 DevBox 独立域名承载，零改动。

```nginx
# 仅在坚持走子路径时使用（同步阻塞，务必放宽超时与 body 上限）
location /qc/ {
    proxy_pass http://<devbox-地址>:8000/;   # 末尾斜杠用于去掉 /qc 前缀
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_buffering off;
    client_max_body_size 100m;       # 默认 1m 会拦掉上传（服务上限 80MB）
    proxy_read_timeout 600s;         # 质检同步执行，务必放宽
    proxy_send_timeout 600s;
}
```

> 注意：**不要用 gunicorn / uvicorn 托管**。`webapp.py` 是 `BaseHTTPRequestHandler` 实现，不是 WSGI/ASGI 应用，只能作为独立 HTTP 进程 + 反向代理。

### 5.6 DevBox 特有的注意事项

| 项 | 说明 |
|---|---|
| 停止 / 休眠 | DevBox 项目被停止（或长时间闲置）后进程不再运行，公网地址会打不开。作为内部长期入口使用时，需要保证 DevBox 处于运行状态，或按 5.1 末尾建议「发布版本 → 应用管理」转正 |
| `entrypoint.sh` | 只做启动，不放构建；必须前台阻塞（`exec python3 webapp.py`），不要 `&` 后台化 |
| 端口一致性 | DevBox 网络配置端口、`QC_PORT`、`entrypoint.sh` 三处必须都是 `8000` |
| 进程守护 | `entrypoint.sh` 被反复拉起；若手工调试，可用 `nohup python3 webapp.py > ~/qc.log 2>&1 &` |
| 上传大小 | 服务上限 80MB（见 `webapp.py` 的 `MAX_UPLOAD_BYTES`）；若经公司网关，还需同步放宽 `client_max_body_size` |
| 长任务超时 | 质检在请求线程内同步执行，大文件耗时较长。走公司网关时务必放宽读超时；先用小文件验证链路 |
| 任务目录 | `runtime/jobs/` 写在 DevBox 工作目录内，重启保留、重建环境会丢；成功任务不会自动清理，建议加定时任务 `0 3 * * * find ~/QC/runtime/jobs -mtime +7 -delete` |
| 日志 | 只打到 stdout，在 DevBox 终端或面板日志里看；真实堆栈只在异常时输出到 stdout |
| HTTPS | 由 Sealos 统一终止，服务本身只跑 HTTP |

### 5.7 接入时必须补的生产配置（服务本身没有）

| 项 | 现状 | 建议 |
|---|---|---|
| 鉴权 | **完全没有** | DevBox 公网地址一旦暴露任何人都能用。务必：优先只在公司内网/VPN 可达的前提下使用，或在公司网关侧加 SSO / Basic Auth，至少不要把地址外传 |
| 下载链接安全 | `/api/jobs/<32位hex>/download` 无鉴权 | 同上，靠网关兜底；不要外传链接 |
| 任务文件清理 | 只有失败时清理，成功任务**永久保留** | 加定时任务清理 `runtime/jobs/`（见 5.6） |
| 持久化 | 写在 DevBox 工作目录 | 重启保留；重建/删除 DevBox 环境会丢。转正到应用管理时改用 PVC |
| 上传安全 | 无病毒扫描、无文件类型白名单以外的校验 | 网关层加扫描 |
| 日志 | 只打到 stdout | DevBox 终端 / 面板查看即可 |
| HTTPS | 无 | 由 Sealos 统一终止 TLS |
| 数据源合规 | 上传的是教务明细数据 | DevBox 属开发环境，注意账号权限与数据留存；正式使用建议转正到应用管理 |

### 5.8 接入验证清单

先设置变量（换成 DevBox 实际分配的公网地址）：

```bash
BASE=https://qc-xxxx.cloud.sealos.io
cd ~/QC

# 1. 存活
curl $BASE/api/health

# 2. 静态资源（确认不是只把 API 放出来了）
curl -s -o /dev/null -w '%{http_code}\n' $BASE/styles.css   # 期望 200

# 3. 容器内自测（排除端口/监听问题）
curl http://127.0.0.1:8000/api/health

# 4. 班课全链路
curl -X POST $BASE/api/qc \
  -F "business_type=class" \
  -F "standard_department=高中班级部" \
  -F "qc_month=2026-09" \
  -F "schedule=@data/classlesson.xlsx" \
  -F "classinfo_opening=@data/本月次月开课.csv" \
  -F "classinfo_setup=@data/本月设班.csv" \
  -F "classinfo_closing=@data/本月结课.csv"

# 5. 高端全链路
curl -X POST $BASE/api/qc \
  -F "business_type=premium" \
  -F "standard_department=高中一对一部" \
  -F "qc_month=2026-09" \
  -F "schedule=@data/pclv.xlsx"

# 6. 下载（用第 4/5 步返回的 download_url）
curl -OJ $BASE/api/jobs/<job_id>/download
```

浏览器侧再验一遍完整交互：选业务 → 选部门 → 上传 → 出统计 → 下载 Excel 能打开且 `质检结果反馈` 列有黄色高亮。

---

## 6. 二次开发指引

### 6.1 新增一条质检规则（以班级信息为例）

1. 在 `qc.py` 新增 `def check_xxx(df) -> df`，内部读列、算布尔掩码、`result['新规则列'] = mask.astype(int)`，返回新 df。
2. 在 `qc_service.py` 顶部的 import 中加入该函数。
3. 把新列名追加到 `qc_service.CLASSINFO_RULE_COLUMNS`（或 `CLASSLESSON_RULE_COLUMNS` / `PCLV_RULE_COLUMNS`）。
4. 若规则需要按月判断，把函数加入 `run_classinfo_qc` 中带 `qc_month=` 的特判集合。
5. 在 `tests/` 对应文件补用例。

> 第 3 步是关键：所有统计、异常汇总、部门汇总、Excel 首列反馈都由 `*_RULE_COLUMNS` 驱动，漏加则规则白跑。
> 约定：规则列值为 `1/0` 数值，唯一例外是 `封班检查`（文本值），代码中已有专门分支处理。

### 6.2 常见改动落点

| 想改什么 | 改哪里 |
|---|---|
| 部门下拉选项 | `web/static/app.js` 的 `CLASS_DEPARTMENTS` / `PREMIUM_DEPARTMENTS`（前端）+ `qc_service` 的 `PREMIUM_DEPARTMENT` 常量（后端） |
| 上传大小上限 | `webapp.py` 的 `MAX_UPLOAD_BYTES`（当前 80MB）+ 网关 `client_max_body_size` |
| 端口/监听地址 | 环境变量 `QC_HOST` / `QC_PORT` |
| 导出 Excel 样式 | `qc_service._format_output_workbook()` |
| 标化部门映射 | `labeling.py` 的 `COVER_KEYWORDS` 与各分支 |
| 输出 Sheet 名 | `qc_service.run_qc_job()` 内的 `sheet_name=` |

---

## 7. 已知风险与待办

| 编号 | 风险 | 影响 | 处理建议 |
|---|---|---|---|
| R1 | 使用标准库 `cgi` 模块 | Python 3.13 起该模块被移除，服务无法启动 | DevBox 选 Python 运行时锁定 ≤3.12；长期建议替换为 `python-multipart` 等 |
| R2 | 同步阻塞执行，无超时/无队列 | 大文件长时间占用线程，网关易 504/超时 | 网关放宽超时；先用小文件验证；并发量大了再考虑任务队列改造 |
| R3 | 任务文件只增不减 | 磁盘持续增长 | 加定时清理（5.6） |
| R4 | 无任何鉴权 | 数据泄露风险 | DevBox 公网地址不要外传，或在网关层补鉴权（5.7） |
| R5 | 静态资源用绝对路径 | 不能挂子路径反代 | 直接用 DevBox 独立域名（方案 A），或改造为相对路径 |
| R6 | DevBox 停止/重建 | 服务不可用、历史任务文件丢失 | 保持 DevBox 运行；长期通过「发布版本」转正到应用管理并挂 PVC |
| R7 | CLI 入口与 Web 规则可能漂移 | `main.py` 只跑班级信息规则且硬编码 `data/` 路径 | 以 Web 链路为准；`main.py` / `classlesson_main.py` 仅作本地调试 |

---

## 8. 交接速查卡

- **启动**：`python3 webapp.py`（或 `QC_HOST=0.0.0.0 QC_PORT=8000 python3 webapp.py`）
- **依赖**：`pandas` + `openpyxl`，仅此两个
- **入口文件**：`webapp.py`；**核心编排**：`qc_service.py`；**规则**：`qc.py` / `classlesson_qc.py` / `pclv_qc.py`
- **健康检查**：`GET /api/health`
- **任务目录**：`runtime/jobs/<job_id>/`（已 gitignore）
- **测试**：`python3 -m unittest discover -s tests`
- **DevBox 启动**：`entrypoint.sh` 内 `exec python3 webapp.py`，`QC_HOST=0.0.0.0`、`QC_PORT=8000`，且网络配置端口同为 `8000`
- **不能做**：不能用 gunicorn/uvicorn 托管；不能挂子路径（需改前端）；不能上 Python 3.13+；不能裸奔暴露公网
